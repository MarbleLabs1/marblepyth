[project]
name = "marblepyth"
version = "0.1.0"
description = "Pyth Network price feed client for Marble Blockchain services"
requires-python = ">=3.9"
dependencies = ["requests>=2.31"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
